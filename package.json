{
  "name": "tournament-operations-engine",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm --recursive build",
    "lint": "pnpm --recursive lint",
    "typecheck": "pnpm --recursive typecheck",
    "test": "pnpm --recursive test",
    "dev": "pnpm --filter @fifa/web dev"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "pnpm": {
    "overrides": {
      "undici": "^6.27.0",
      "uuid": "^11.1.1",
      "vite": "^6.4.3",
      "esbuild": "^0.25.0"
    }
  }
}
